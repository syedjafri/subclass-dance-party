$('#player').sprite({fps: 12, no_of_frames: 5});

function lineup()={
  // save positions of all elements
  // align everyone
 // $('.blanka').css("position":"fixed", "top": "0");
  
}

function goBack = function(){
  // return everyone back to their positions
}